"use client";

/** Colocated error boundary (AGENTS.md rule 6). Thin re-export of the locale-level
 *  boundary — the level word counts are a server read and throw by design. */
export { default } from "../../../error";
